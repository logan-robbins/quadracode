[pytest]
markers =
    integration: tests requiring live backing services (Redis, agents, orchestrator)
    e2e: End-to-end scenario exercising the full stack.
    e2e_advanced: Long-running advanced E2E tests with real LLM calls (5-20 minutes each)
    long_running: Tests that run for 5+ minutes (subset of e2e_advanced)
