# syntax=docker/dockerfile:1
# Quadracode orchestrator Redis MCP server image
# Uses the official MCP Redis server from https://hub.docker.com/r/mcp/redis

FROM mcp/redis:latest

# The upstream image already exposes the MCP server on port 8000 and redis on 6379.
# No additional configuration required; runtime configuration is provided via env.
